module.exports = {
  getThreadById: (id) => {
    //
    thread = {};
    return thread;
  },

  insertThread: (threadParent, threadChild) => {

    
    if (false) {
      throw "Could not insert thread into DB";
    }
  },

  getThreads: (params) => {
    //query
    let threads = [];
    return threads;
  }
}
